<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mapper.StorageEntitlementMapper">

    <resultMap id="BaseResultMap" type="com.app.pojo.entity.StorageEntitlementEntity">
        <result column="id" jdbcType="BIGINT" property="id"/>
        <result column="entitlement_no" jdbcType="VARCHAR" property="entitlementNo"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="source_type" jdbcType="VARCHAR" property="sourceType"/>
        <result column="payment_order_id" jdbcType="BIGINT" property="paymentOrderId"/>
        <result column="name_snapshot" jdbcType="VARCHAR" property="nameSnapshot"/>
        <result column="capacity_bytes" jdbcType="BIGINT" property="capacityBytes"/>
        <result column="duration_value" jdbcType="INTEGER" property="durationValue"/>
        <result column="duration_unit" jdbcType="VARCHAR" property="durationUnit"/>
        <result column="user_time_zone_snapshot" jdbcType="VARCHAR" property="userTimeZoneSnapshot"/>
        <result column="effective_time" jdbcType="TIMESTAMP" property="effectiveTime"/>
        <result column="expire_time" jdbcType="TIMESTAMP" property="expireTime"/>
        <result column="status" jdbcType="VARCHAR" property="status"/>
        <result column="expired_processed_time" jdbcType="TIMESTAMP" property="expiredProcessedTime"/>
        <result column="create_by" jdbcType="VARCHAR" property="createBy"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_by" jdbcType="VARCHAR" property="updateBy"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, entitlement_no, user_id, source_type, payment_order_id, name_snapshot, capacity_bytes, duration_value, duration_unit, user_time_zone_snapshot, effective_time, expire_time, status, expired_processed_time, create_by, create_time, update_by, update_time
    </sql>

    <select id="selectByIdForUpdate" resultMap="BaseResultMap" parameterType="long">
        SELECT <include refid="Base_Column_List"/> FROM t_storage_entitlement WHERE id = #{id} FOR UPDATE
    </select>

    <update id="updateStatusById">
        UPDATE t_storage_entitlement SET status = #{status}, update_time = CURRENT_TIMESTAMP(3) WHERE id = #{id} AND status = #{expectedStatus}
    </update>

    <select id="selectActiveByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM t_storage_entitlement
        WHERE user_id = #{userId} AND status = 'ACTIVE' AND effective_time &lt;= #{now} AND expire_time &gt; #{now}
        ORDER BY expire_time ASC, id ASC
    </select>

    <select id="selectExpired" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM t_storage_entitlement
        WHERE status = 'ACTIVE' AND expire_time &lt;= #{now} AND expired_processed_time IS NULL
        ORDER BY expire_time ASC, id ASC LIMIT 100
    </select>

    <select id="sumActiveCapacity" resultType="long">
        SELECT COALESCE(SUM(capacity_bytes), 0) FROM t_storage_entitlement
        WHERE user_id = #{userId} AND status = 'ACTIVE' AND effective_time &lt;= #{now} AND expire_time &gt; #{now}
    </select>

    <update id="markExpired">
        UPDATE t_storage_entitlement SET status = #{status}, update_time = CURRENT_TIMESTAMP(3)
        WHERE id = #{id} AND status = 'ACTIVE'
    </update>

    <update id="markProcessed">
        UPDATE t_storage_entitlement SET expired_processed_time = #{processedTime}, update_time = CURRENT_TIMESTAMP(3)
        WHERE id = #{id} AND status = 'EXPIRED' AND expired_processed_time IS NULL
    </update>

</mapper>
